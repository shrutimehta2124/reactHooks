const UseContextHook = () => {
    return (
        <div>
            <h3>useContext Hook</h3>
            <p>useContext provides a way to pass data through the component tree without prop drilling.</p>
        </div>
    );
};

export default UseContextHook;
