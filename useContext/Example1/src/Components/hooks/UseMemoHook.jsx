const UseMemoHook = () => {
    return (
        <div>
            <h3>useMemo Hook</h3>
            <p>useMemo memoizes values to avoid unnecessary calculations on re-render.</p>
        </div>
    );
};

export default UseMemoHook;
